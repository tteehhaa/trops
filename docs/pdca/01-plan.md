# PLAN — A-1 / A-3 / B-1 / B-3 (2026-08-11)

08-11 잔여 작업 검토 결과 네 항목에 대한 계획입니다.
문서 위치를 `docs/pdca/` 로 잡은 이유: `docs/` 최상위는 정본(문구·디자인·와이어프레임)
전용이고, 작업 사이클 문서가 섞이면 「현행 정본이 무엇인가」가 흐려집니다.
`docs/` 전체가 `.vercelignore` + `build-static.js NOT_DEPLOYED` 라 배포되지 않습니다.

## 1. 현재 상태 · 의존관계 · 블로커

| 항목 | 현재 상태 | 블로커 | 이 저장소에서 완결 가능? |
|---|---|---|---|
| **A-1** 통신판매업신고번호 | 6개 HTML 이 `신청 중`/`Applied for` 를 **각자 하드코딩**. `index.html:1327` · `nda.html:271` · `precheck.html:775` · `refund.html:390` · `uae.html:427` · `en.html:1314` | 신고번호 **값** 자체는 신고 수리 대기(외부) | ✅ **구조는 가능** — 값은 못 채우지만 「한 곳만 고치면 되는 구조」는 지금 만들 수 있음 |
| **A-3** 개인정보처리방침 + 동의 | 방침 페이지 자체 부재. §10 폼(`index.html:1261` · `en.html:1247`)은 name/email/company 3필드뿐. `/api/leads` 도 3필드만 받음 | 없음 (법무 확정 문구는 DRAFT 배너로 분리 가능) | ✅ |
| **B-1** IPO 기준 엔진 전환 | 랜딩 표기는 국·영문 모두 반영됨(`index.html:1013,1166` · `en.html:1001,1159`). 기준 서식 데이터·대조 로직 **부재** | 판정층이 이 저장소에 없음 | ❌ |
| **B-3** §7 준거법·관할 | 페이지 반영 0. `index.html:1006` 은 HTML 주석뿐 | trops_a **미결 L-1**(4상태 매핑) 결정 대기 | ❌ |

## 2. 스코프 인 / 아웃 결정

### 스코프 인 — A-1, A-3

둘 다 이 저장소가 소유한 파일(HTML 6개 · `api/leads.js` · `scripts/build-static.js`)
안에서 시작해서 끝납니다. 외부 결정을 기다리지 않습니다.

A-1 에 대해 짚어 둘 것: **값과 구조를 분리합니다.** 신고번호 확정은 외부 사건이지만,
「6곳에 흩어진 하드코딩」은 그 사건과 무관한 이 저장소의 구조 문제입니다.
지금 구조를 고쳐 두면 번호가 나온 날 할 일이 **6파일 편집 → 1값 수정**으로 줄어듭니다.
반대로 지금 미루면 신고 수리일에 6곳을 손으로 고치게 되고, 그때가 가장 실수하기 쉬운
시점입니다(현행 주석 6개가 전부 "번호로 교체하십시오" 라고 적혀 있는 상태).

### 스코프 아웃 — B-1, B-3

**B-1 (IPO 기준 엔진)** — 이 저장소의 경계 밖입니다.
`api/_supabase.js` 머리주석이 경계를 명시합니다:

> ⚠️ 경계: 이 폴더(main_web_page)는 접수·저장·알림만 담당합니다.
>    LLM 호출·상태 판정 코드를 두지 않습니다. (판정층은 trops_a 에 분리되어 있습니다.)

기준 서식(IPO/OGL 18개 항목) 데이터와 대조 로직은 판정층 자산입니다. 여기에 만들면
그 경계가 무너지고, 같은 기준이 두 저장소에 생겨 드리프트가 시작됩니다.
**이 저장소 몫(랜딩 표기)은 이미 국·영문 모두 끝나 있습니다** — 08-11 검토에서
「부분구현」으로 나온 것은 엔진이 없어서지 랜딩이 모자라서가 아닙니다.
→ 코드 변경 없음. 원장(`docs/README.md`)에 경계와 완료 범위만 기록.

**B-3 (§7 준거법·관할)** — 선행 결정 미결입니다.
`docs/README.md` 미결표에 「준거법·관할의 4상태 매핑 | 정본 §7 · 미결 L-1」로 등재돼
있고, 같은 문서에 trops_a 가 이번 캡처 라운드에서 §7 준거법 문구를 **완전히 빼기로**
방향을 틀었다고 적혀 있습니다. 4상태 매핑이 확정되기 전에 화면 표현을 만들면
정본 §7 이 금지한 「같음/다름 2상태」로 굳어질 위험이 그대로입니다
(정본 v3 §7: "「다름」으로 찍으면 사용자는 '우리 NDA 준거법이 잘못됐나'로 읽는다.
근거 없는 판정이 된다"). 게다가 표현이 들어갈 **결과지 화면 자체가 이 저장소에 없습니다.**
→ 코드 변경 없음. 원장에 종속 관계만 기록.

## 3. 실행 순서와 완료 기준(DoD)

### 배치 1 — A-1 단일소스화
1. `site.config.json`(루트) 신설 — 사업자정보 6항목 × 국/영문
2. `scripts/build-static.js` 에 토큰 치환 단계 추가 + 미해결 토큰 시 빌드 실패
3. HTML 6개의 `.footer-biz` 값 + 법인명 반복을 토큰으로 교체
4. `test/site-config.test.js` — 값 하나 바꾸면 6개 산출물이 전부 바뀌는지

**DoD**: `site.config.json` 한 줄만 고치고 `npm run build` → dist 6개 파일 전부 반영.
치환 전후 렌더링 문구 동일. `npm test` 전부 통과.

### 배치 2 — A-3 개인정보처리방침 + 동의
1. `privacy.html`(국) · `en-privacy.html`(영) 신설 — DRAFT 배너 + 실제 수집 필드만 기술
2. 6개 파일 푸터에 방침 링크 추가
3. §10 폼(국·영문)에 필수·선택 동의 체크박스 2종 + 클라이언트 검증
4. `/api/leads` 확장 + 서버측 필수 동의 검증
5. `test/leads-consent.test.js`
6. `en.html:1241` · `en.html:1299` 등 「페이지 없음/법무 미결」 주석 갱신

**DoD**: 필수 미체크 → 클라이언트가 제출 차단, 우회해도 서버가 400.
방침 페이지가 실제 수집 필드(name/email/company + 동의값)와 일치. 국·영문 동일.

### 배치 3 — B-1 · B-3 원장 기록
`docs/README.md` 미결표에 스코프 아웃 사유 추가. 코드 변경 없음.

**DoD**: 다음 사람이 원장만 읽고 「왜 이 저장소에 없는가」를 알 수 있음.

## 4. Supabase 판단

`/api/leads` 는 **Supabase 를 쓰지 않습니다** — Resend 로 메일 두 통을 보내고 끝입니다
(`api/leads.js:1-70`). Supabase 를 쓰는 것은 `api/intake.js`(사전확인 접수)뿐이고
이번 변경과 무관합니다. → **스키마 변경·마이그레이션 없음.**
