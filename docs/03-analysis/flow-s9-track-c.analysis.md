# flow-s9-track-c Gap Analysis

> **Date**: 2026-08-13
> **Plan**: `docs/01-plan/features/flow-s9-track-c.plan.md`
> **Design**: `docs/02-design/features/flow-s9-track-c.design.md`
> **Spec**: `doc/s9/TROPS_user_flow_2026-08-13.md`
> **Match Rate**: **97 / 100** (게이트 90 통과)

---

## 배점 요약 (사용자 지정 기준)

| 축 | 배점 | 획득 | 근거 |
|---|---|---|---|
| 랜딩 1~4번 정상동작 | 25 | **25** | 4건 전부 구현·검증. S3 는 이미 스펙 일치라 무수정 |
| precheck 5~7번, 로그인필드 추가 0건 | 30 | **30** | `docType` 5계층 왕복 완결 · 로그인 필드 0건을 테스트가 단정 |
| 이메일 8~9번 발송 트리거 | 25 | **22** | 배선·후보선정·멱등은 테스트로 단정. **라이브 발송 실측 미실행** (−3) |
| 기존 테스트 회귀 없음 | 20 | **20** | 244 → 318 전건 통과 · 빌드 성공 · dist 금칙어 0건 |
| **합계** | **100** | **97** | |

---

## 스코프별 결과

| # | 항목 | 상태 | 실측 근거 |
|---|---|---|---|
| 1 | KOTRA 문구 위치 확인/이동 | ✅ 이동 | `#interest .interest-sub` → `.close-cta-kotra`. dist 실측 노출 **1회** |
| 2a | 신뢰증명 통계블록 등장 애니메이션 | ✅ 신규 | `.reveal` + IntersectionObserver. 숫자 카운트업이 아니라 **문장 페이드업**(아래 결정 D-A) |
| 2b | 기한관리 지도 핀 순차 모션 | ✅ 신규 | `@keyframes pin-drop` + `--i` 지연(0·160·320ms). 핀 3개 |
| 2c | 아코디언 슬라이드 | ✅ **기존** | `grid-template-rows: 0fr→1fr` `.24s` — 이미 구현돼 있었음. 무수정 |
| 3 | 아코디언 카드 3종 인터랙션 | ✅ **기존** | 3종 전부 스펙 일치. 무수정 (아래 재검증 표) |
| 4 | FAQ 채널분기 문항 | ✅ 신규 | `qa-14`, 「어떻게 쓰나요」 묶음. 13 → 14문항 |
| 5 | 문서유형 선택 필드 | ✅ 신규 | 화면 select · `parseDocType` · `doc_type` 컬럼 · 메일 · 확인화면 5계층 |
| 6 | SLA 문구 | ✅ 신규 | `.pay-sla` 「영업일 기준 24시간 내에 전달해 드립니다」 |
| 7 | 진행상태 3단계 | ✅ 신규 | `progressStage()` — 2단계 근거를 `precheck_nda_run` 행 존재로 |
| 8 | 결제확인 메일 기한관리 링크 | ✅ 신규 | `waitingRoomHtml()`, 무상·유료 공용 경로에 삽입 |
| 9 | 결제 미완료 리마인드 | ✅ 신규 | `api/_payment-reminder.js` + cron 3번째 잡 + CLI + 스키마 |

### 3번 재검증 결과 (수정 0건)

| 카드 | 스펙 | 실측 | 판정 |
|---|---|---|---|
| 사전점검 | 실행버튼 없음 + 설명 + 예시화면 | 실행버튼 0개 · `.feat-desc` · `/img/c03-result.jpg` | ✅ 일치 |
| 바이어확인 | 실행버튼 없음 + 연결 안내문만 | 실행버튼 0개 · 「사전점검 결과화면에서 바로 확인하실 수 있습니다.」 | ✅ 일치 |
| 기한관리 | [계약 등록해보기] → app.trops.kr | `btn-secondary` 1개 · `href="https://app.trops.kr/"` · **HTTP 200 실측** | ✅ 일치 |

---

## 스펙과 의도적으로 다르게 한 것 (근거 있음)

| # | md 원문 | 실제 구현 | 근거 |
|---|---|---|---|
| D-A | §1 「숫자가 스크롤 진입 시 0→목표값 카운트업」 | 문장 페이드인+슬라이드업 | 이 블록은 숫자가 아니라 **무보 인용 문장**이다(미결 L-1 해소 시 결정). 확인 안 된 숫자를 크게 띄우는 것은 바로 위 h2(「무엇을 근거로 비교하는지 밝힙니다」)와 정면으로 어긋나고, 기존 주석이 카운트업을 **명시적으로 금지**하고 있었다. 「스크롤 진입 시 등장」이라는 요구는 그대로 지켰다 |
| D-B | §1 「실제 대시보드 세계지도(마스킹 샘플)」 위의 핀 | 지도 없는 추상 핀 3개 + 「캡처 준비 중」 유지 | 캡처 에셋이 `img/` 에 아직 없고, `.feat-map` 주석이 **가짜 지도 그리기를 금지**한다(제품 화면이 아닌 것을 제품 화면처럼 보이게 하지 않는다). 사용자 스코프도 「자리표시자든 실물이든」으로 허용. 핀 애니메이션은 캡처 반영 시 **좌표만 옮기면 그대로 재사용**되도록 남겼다 |
| D-C | §3 「결제화면 복귀 링크(재업로드 불필요)」 | `/precheck` 로 보내고 「한 번 더 올려주셔야 합니다」로 명시 | 주문 재사용(결제 재개)이 구현돼 있지 않다. 「재업로드 불필요」를 쓰면 눌러 들어온 사람이 빈 폼을 보고 **두 번 이탈**한다. 지키지 못할 약속을 쓰지 않는다 — 테스트가 이 문구를 금지로 단정 |
| D-D | §3 「N시간(3시간) 내 결제 미완료 시」 | 적격 임계값 3h, 발송 주기 1일 1회 (최대 24h 지연) | Hobby 플랜 cron 은 분·시 고정(하루 1회)이고 `test/cron-registration.test.js` 가 그것을 단정한다. 상한 2개도 이미 소진 상태였다 |
| D-E | §5-1 10번 「검토중」 | 문구는 그대로 쓰되 **근거를 `status` 가 아니라 대조 실행 증적에서** 읽음 | `status='in_progress'` 는 사람이 손대지 않은 건에도 붙는다(기존 `statusLabel` 주석의 실측). 그것으로 「검토중」을 말하면 아무도 안 보는데 보고 있다고 말하는 것이 된다. `precheck_nda_run` 행이 있으면 그 문장이 **사실**이 된다 |

---

## 발견·수정한 실제 버그 1건

### `.steps` / `.step` 클래스 충돌 (내가 만든 것 → 즉시 수정)

진행상태 트래커를 처음 `.steps`/`.step` 으로 썼는데, precheck.html 위쪽 「이용 방법」 3단계 섹션이 **그 두 이름을 이미 쓰고 있었다**(`.step { display: grid; grid-template-columns: … }`). 나중에 선언된 `display: flex` 가 그 그리드를 덮어써, 접수확인 화면이 아니라 **관계없는 섹션의 레이아웃**이 깨지는 상태였다.

- 발견 경로: `test/progress-stage.test.js` 의 색상 검사가 red 를 냈고, 원인을 보니 색이 아니라 선택자 범위였다
- 수정: `.progress` / `.progress-step` / `.progress-dot` / `.progress-mark` / `.progress-label` 로 전면 개칭
- 재발 방지: 「addProgress 가 붙이는 클래스는 `progress`/`progress-*` 뿐」 + 「이용 방법 `.step` 의 grid 가 살아 있는가」 두 단정을 테스트에 박음

## 발견한 기존 문서 오류 1건 (정정)

`index.html` FAQ 섹션 주석이 3주 동안 **「3묶음 14문항」으로 잘못 적혀 있었다.** 실제 `.qitem` 은 13개(3+5+5)였다. 채널분기 문항을 더해 지금 실제로 14문항이 됐고, 주석에 실측 정정 경위를 남겼다. 문항 수는 이제 테스트가 단정한다.

---

## 테스트 추가 내역 (+74건)

| 파일 | 건수 | 중심 단정 |
|---|---|---|
| `test/doc-type.test.js` | 16 | 준비중 종류를 **조용히 `nda` 로 눕히지 않는다** / 거절이 슬롯·파일을 남기지 않는다 / 화면·서버·DB 세 목록 일치 / **로그인 필드 0건** |
| `test/progress-stage.test.js` | 12 | `in_progress` 만으로 검토중이 되지 않는다 / 결제 전·취소는 null / 화면이 판단을 복제하지 않는다 / **클래스 충돌 방지** |
| `test/payment-reminder.test.js` | 16 | 후보 5조건 / **72h 상한** / 발송 실패 시 표시 안 함 / 미리보기가 아무것도 안 보냄 / CLI↔cron 동일 본체 |
| `test/waiting-room-mail.test.js` | 12 | 링크가 **실재하는 앵커**를 가리킨다 / 훅 문장이 랜딩과 같다 / 메일·화면 SLA 값 일치 / 지키지 못할 약속 금지 |
| `test/landing-flow-s9.test.js` | 18 | KOTRA 1회 노출 / **JS 죽어도 인용문 보임** / 핀 순차 / 가짜 지도 없음 / 카드 3종 스펙 / FAQ 짝·수 |

`npm test`: **244 → 318, 전건 통과. 실패 0.**

---

## 남은 갭 (−3점의 내용)

1. **라이브 발송 실측 미실행** — 8·9번의 메일 트리거는 가짜 `fetch`/Resend 대역 위에서만 검증됐다. 실제 발송은 아래 선행 작업이 끝난 뒤 1건 실측이 필요하다.
2. **스키마 마이그레이션이 선행 필수** — `precheck-schema.sql` 「0-H」 절(`doc_type` · `payment_reminder_sent_at`)을 **프로덕션 Supabase 에서 먼저 실행해야 한다.** `doc_type` 은 접수 경로가 실제로 insert 하는 컬럼이라, 컬럼이 없으면 PostgREST 가 PGRST204 로 거절하고 **접수 전체가 502** 가 된다. 다른 0-* 절(환불 컬럼 등)처럼 「운영 도구만 멈춘다」가 아니다.
3. **리마인드 첫 실행 전 미리보기 권장** — `npm run remind:preview` 로 대상 건수를 먼저 세어 볼 것. 72h 상한이 막고 있지만, 그 상한이 실제로 몇 건을 남기는지는 확인하고 켜는 것이 맞다.
4. **en.html 미동기화** — KOTRA 문구·FAQ 문항이 국문에만 들어갔다(트랙A 선례와 동일한 국문 한정 배치). `en.html` 은 `.interest-sub` 마크업과 CSS 사본을 그대로 갖고 있다.
5. **가격 ₩99,000 → ₩300,000 미반영** — 이번 스코프 외. `verify-deployment.js` 의 R1-취소선이 `₩99,000` 존재를 단정하므로 함께 고쳐야 하는 별건이다.
