# Analysis (Check) — landing-b1-facts-freecopy

| 항목 | 내용 |
|---|---|
| 일자 | 2026-08-23 |
| Plan | `docs/01-plan/features/landing-b1-facts-freecopy.plan.md` |
| Design | `docs/02-design/features/landing-b1-facts-freecopy.design.md` |
| 통과 기준 | **90점** 미만 통과 불가 |
| **Match Rate** | **96 / 100 — PASS** |

## Context Anchor

| 축 | 내용 |
|---|---|
| **WHY** | 랜딩의 사실 오류 2건 + 종료된 무상 제공 문구 제거 (PRD §1). |
| **WHO** | 랜딩 방문자 + 토스 결제 심사자. |
| **RISK** | 가격·결제 diff 1줄 = fail. 슬롯 엘리먼트 삭제 = 접수 정지. |
| **SUCCESS** | 절대 조건 5개 + 기존 검사 green + ≥ 90. |
| **SCOPE** | 정적 문구 · 설정 1값 · 테스트 3파일 · 문서. Supabase 무변경. |

## 1. 절대 조건 (AC) — 전건 기계 검증

| AC | 조건 | 결과 | 증거 |
|---|---|:--:|---|
| **AC-1** | `precheck.html`·`en-precheck.html` 가격·결제 요소 diff 0줄 | ✅ | `check-b1-gates.js` G1 — `HEAD` 판과 보호 구간(패턴 줄 + 결제 폼 블록) **바이트 동일**. 육안 확인: 두 파일의 실제 변경 12줄에 `plan-price`·`pay-summary-value`·`pay-vat`·`₩330,000`·결제 버튼·`.pay-area` 가 **한 줄도 없음** |
| **AC-2** | 금지 문자열 5종 저장소 전체 0건 | ✅ | G2 — 추적 + 미추적 파일 280개 스캔, 0건. `dist/` 는 G5 가 별도로 0건 |
| **AC-3** | `precheck.itemCount = 17` | ✅ | G3 + `item-count.test.js` |
| **AC-4** | `index.html`·`en.html` 알림 주기 = 「30일 전, 7일 전, 1일 전」 | ✅ | G4 (양 파일). 영문 본문은 `30 days, 7 days, and 1 day` |
| **AC-5** | 기존 검사 전량 green + 부재 검사 신설 | ✅ | `npm test` → **541 pass / 0 fail**. 신설: `price-exposure.test.js` 「종료된 무상 제공 문구가 저장소 어디에도 없다」 |

**Supabase**: 스키마·데이터·`api/` 변경 0건. `git diff --stat` 에 `api/`·`*.sql` 없음. ✅

## 2. 점수

| 축 | 배점 | 획득 | 근거 |
|---|:--:|:--:|---|
| 절대 조건 (AC-1~5) | 50 | **50** | 5/5, 전건 기계 검증 |
| 설계 준수 (Option C) | 20 | **20** | 문구만 치환, 실행 로직 0줄. 게이트 스크립트 신설 |
| 회귀 방지 | 15 | **15** | 신설 검사가 저장소 전체를 봅니다 — `*.html` 만 보던 PRD 기준보다 강합니다 |
| PRD B1 항목 이행 | 15 | **11** | B1-1·2·3·5·6 완료. **B1-4 보류** (아래 §3) |
| **합계** | **100** | **96** | **PASS (≥ 90)** |

## 3. Gap — 의도적 편차 1건

### G-1. PRD B1-4 (`en-precheck.html:1070` 로그인불필요 문구 삭제) — **보류**

- **사실**: 해당 줄의 실제 문면은 `Pay by credit or debit card. No login or account required.` 로, 금지 문자열(`No login` + `required` 인접형)과 **다릅니다**. AC-2 위반이 아닙니다.
- **충돌**: 이 줄은 `<div class="pay-area">`(결제 폼) **안**입니다. 손대면 **AC-1 이 깨집니다.**
- **판단**: 사용자가 지정한 절대 조건 1번(토스 심사 · 결제 폼 diff 0줄)이 PRD 세부 항목보다 우선합니다. 보류하고 PRD 본문에 사유를 기록했습니다.
- **후속**: 토스 심사 종료 후 B2/B3 에서 처리 (PRD §8-2 미결 4번과 같은 해제 조건).

## 4. 설계 대비 확인

| 설계 항목 | 이행 |
|---|:--:|
| §3 보호 구간 바이트 비교 게이트 | ✅ `scripts/check-b1-gates.js` G1 |
| §4 문구 매핑 14행 | ✅ 전행 |
| §5.1 금지 문자열 리터럴 미기재 | ✅ `RETIRED_OFFER_PHRASES` 조각 조립 — 검사 파일 자신이 걸리지 않음 |
| §5.2 기존 기대값 갱신 2건 | ✅ `naming-consistency` · `item-count` |
| §6 읽기 실패를 조용히 넘기지 않음 | ✅ `unreadable` 배열로 명시 실패 |
| §7 Supabase 무변경 | ✅ |

**설계 중 발견해 고친 실수 1건**: 첫 게이트 판이 `git ls-files` 기본 출력을 썼는데, 한글 경로가 `"…\353…"` 로 따옴표 인용돼 나와 **한글 경로 문서 11개를 조용히 건너뛰었습니다**(24건 → 실제 83건). `-z` 로 고쳤고, 같은 함정을 `price-exposure.test.js` 주석에 남겼습니다. 이 배치에서 가장 위험했던 자리입니다 — 고치지 않았다면 게이트가 **통과하는 척**만 했습니다.

## 5. 문서 처리 방침

`docs/` 의 옛 원고·와이어프레임·아카이브 11개 파일은 **삭제가 아니라 치환**했습니다(예: 종료된 프로모션 표기 → 「초기 20건 무상」). 사유:
- 사용자 절대 조건 2번은 「저장소 전체 0건」이라 아카이브도 예외가 아닙니다.
- 그러나 결정 이력을 지우면 다음 사람이 근거 없이 되돌립니다. 원문은 **git 이력에 그대로 보존**되므로 치환으로 양쪽을 다 지킵니다.
