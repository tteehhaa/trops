# TROPS 랜딩 디자인 실행 통합본 v4

2026-08-10 (v1) / 2026-08-11 (v2/v3 정정) / 2026-08-16 (v4 · 라이브 동기화) ·
**디자인 실행 정본**

선행 문서: `docs/copy/trops_랜딩_최종문구_확정본_v4.0.md`(문구·근거) ·
`trops_landing_visual_spec_v1.md`(시각 사양, 옛 9섹션 구조 기준이라 배치도는 참고만) ·
인계 브리프 · 5지점 회신

**v4 변경분(2026-08-16)**: 기준을 "구현이 정본을 따른다"에서 **"정본이 구현을
따른다"로 뒤집었습니다.** `index.html`이 라이브 기준이고, 이 문서는 그 상태를
그대로 옮겨 적었습니다. §2 섹션 구조가 13개→15개로, §4 컴포넌트가 카드+아코디언
구조에서 탭 구조로 전면 개편됐고, §3-1 색 토큰 3종이 값째로 바뀌었습니다.
옛 판(v2·v3)은 `docs/design-master/archive/`로 옮겼습니다 — 참고용, 따르지 마십시오.

**v2 변경분(참고, archive)**: §3-2 eyebrow 색 예외 규정 · §9 자간 표기 정정.
**v3 변경분(참고, archive)**: §C-3 QnA 13→14문항 · §C-4 기관 나열 순서 정정.

**2026-08-16 마무리 정리 4건 반영** (파일명·버전은 유지, 본문만 갱신) — 상세는
`docs/copy/trops_랜딩_최종문구_확정본_v4.0.md` 머리말과 동일: ① 로드맵01 상품명
"확인 항목 대조표" ② QnA 채널분기 문항 "수출 사전점검" ③ 푸터 태그라인 갱신
(C-2·C-3 참조) ④ CEPA는 다루지 않음 — §12 미결에 항목만 추가.

---

## 0. 이 문서의 위치

| 층 | 정본 | 이 문서와의 관계 |
|---|---|---|
| **문구** | `docs/copy/trops_랜딩_최종문구_확정본_v4.0.md` | 여기서 복사·요약. **원문이 우선** |
| **시각·구조** | 이 문서 §3~§6 | 여기가 정본이나, 값의 최종 출처는 항상 `index.html` `:root` |
| **구조** | 이 문서 §2 | `index.html` 섹션 순서를 그대로 옮김 |
| **법무·행정** | 문구 확정본 §15 | 참조만 |

**문구는 한 글자도 수정하지 않는다.** 바꿔야 할 이유가 생기면 바꾸지 말고
마케팅팀에 문의.

---

## 1. 이 페이지가 하는 일

수출 3년 이내 · 해외영업 0~2명 · 사내 매뉴얼 없는 중소기업 대표가, 바이어에게
받은 서류를 올려 공개 표준 서식과 비교받는다. 지금은 사전점검·바이어 확인·기한
관리 세 상품을 판다(v1~v3 시점에는 사전점검 하나뿐이었다 — §2 참조).

**90일 안에 무료 20건을 채우는 것**이 유일한 목표였던 v1~v3와 달리, 라이브는
가격·오픈월 표기를 걷어냈다(§8 준비 중인 것). 지금 목표 수치는 이 저장소
문서만으로는 확인할 수 없다 — §12 미결 참조.

읽는 사람은 화면을 오래 들여다보지 않는다. **밀도보다 순서가 중요하다** —
이 원칙은 유지된다.

---

## 2. 섹션 구조 15개 (라이브 기준)

v3 시점 13개(신설 2개는 예정 상태였던 CEPA·신뢰 이미지 포함)에서, 라이브는
CEPA 섹션을 완전히 제거하고 신뢰·근거를 분리하고 경험담·결 CTA를 신설해
**15개**가 됐다.

| # | 섹션 | 라이브 클래스/id | 배경 | 구(v3) 대응 |
|---|---|---|---|---|
| 1 | 헤더 | `nav.nav` | `--bg` | 1 헤더(단, CTA 버튼 없음 — 완전 강등) |
| 2 | 히어로 | `.hero` | `--bg` | 2 히어로 |
| 3 | 있었던 이야기 | `.stories-sec` | `--surface` | ★신설 |
| 4 | 신뢰(통계 인용) | `.trust` | `--surface-dark` | 7 신뢰(내용은 분리, 이 섹션엔 통계 1문장만) |
| 5 | 상품소개(탭 3개) | `.cards-sec#service` | `--bg` | 4 2층카드 + CEPA 통합·탭화, CEPA는 삭제 |
| 6 | 근거 | `.basis` | `--surface` | 구 7 신뢰의 근거 3줄+이미지가 분리돼 옮겨옴 |
| 7 | 결 CTA | `.act` | `--bg` | ★신설(구 10 마감CTA의 주버튼 역할을 흡수) |
| 8 | 안심 문구 | `.assure` | `--surface` | 3 안심 문구(히어로 직하 → 여기로 재배치, H2 신설) |
| 9 | HOW IT WORKS | `.how#how` | `--bg` | 5 HOW(자리만 이동) |
| 10 | 준비 중인 것 | `.how.sec-surface#next` | `--surface` | 8 로드맵(이름 변경, 가격 삭제) |
| 11 | QnA | `.qna#qna` | `--bg` | 9 QnA(자리 이동, 3+6+5로 재구성) |
| 12 | 마감 CTA | `.close-cta` | `--surface` | 10 마감 CTA(H2·버튼 전면 교체, 주버튼 삭제) |
| 13 | 사전 등록 폼 | `.interest#interest` | `--bg` | 11 사전 등록 폼(목적선택 신설, 문서선택 삭제) |
| 14 | 기관 안내 | `.orgs-sec#orgs` | `--surface` | 12 기관 안내 |
| 15 | 푸터 | `footer.footer` | `--surface-dark` | 13 푸터 |

### 2-1. 정렬 교차 리듬

배경 교차는 `.sec-surface` 클래스 하나로 통제한다(§3-1). 순서: **bg → surface →
dark → bg → surface → bg → surface → bg → surface → bg → surface → bg → surface
→ dark.** 다크는 페이지에 정확히 둘, 4(신뢰)와 15(푸터)뿐이다 —
`test/landing-order-s9.test.js` O7이 "배경이 연속인 이웃이 없다"를 센다.

### 2-2. 우선순위 (라이브가 실제로 실행한 순서)

옛 "3→4→7"(안심문구 승격 → 2층카드 → 신뢰 이미지) 대신, 라이브 코드주석이 실제로
따른 감정선은 **기(2 히어로) → 승(3 경험담) → 전(4 신뢰경고 · 5 상품소개 · 6
근거) → 결(7 결 CTA)**이다. 안심 문구(8)는 그 **뒤**, "누르지 않은 사람의
망설임에 답하는" risk-reversal 자리로 옮겨졌다 — 이는 옛 우선순위 1번("안심
문구를 맨 위에서 한 번")과 정반대 배치다. 옛 표를 참고하지 말 것.

### 2-3. 앵커 연결

| 출발 | 도착 |
|---|---|
| 헤더 "서비스" | 5 상품소개 (`#service`) |
| 헤더 "이용 방법" | 9 HOW (`#how`) |
| 헤더 "자주 묻는 질문" | 11 QnA (`#qna`) |
| 9 HOW 03단계 "어디에 물어보시면" | 14 기관 안내 (`#orgs`) |
| 11 QnA "어디에 물어볼 수 있나요" | 14 기관 안내 (`#orgs`) |
| 히어로·10·12의 "기한관리 미리보기" | 5 상품소개 탭3 패널(`#feat-timeline-panel`, 페이지 이동 아님 — 펼치고 스크롤) |

v3의 "12번이 폼 뒤로 내려가며 5·9와 멀어져 앵커 두 개로 다리를 놓는다"는
설명은 유효하나, 지금은 "기한관리 미리보기" 트리거가 **세 곳**(히어로·10·12)에서
같은 패널 하나를 공유하는 구조가 추가됐다 — 패널을 복제하지 말 것.

---

## 3. 디자인 토큰

### 3-1. 색 (🔴 v3에서 값이 바뀐 것 3종 — `index.html` `:root` 원문 기준)

```css
/* 잉크 — 4단계, 변경 없음 */
--ink:    #0F172A;
--ink-70: rgba(15,23,42,.70);
--ink-64: rgba(15,23,42,.64);
--ink-62: rgba(15,23,42,.62);
--muted:  #94A3B8;

/* 액센트 — 클릭 가능한 요소에만, 변경 없음 */
--accent:       #1D4ED8;
--accent-press: #1E40AF;
--accent-soft:  #EFF6FF;

/* 배경 — 3층 */
--bg:           #FFFFFF;
--surface:      #E6ECF3;   /* 🔴 v3 값 #F1F5F9(ΔL* 3.65)에서 재변경. 3.65도 교차가
                               안 잡혀 6.86으로 한 번 더 내렸다(2026-08-14) */
--surface-dark: #0F172A;   /* 변경 없음 */

/* 선 */
--line:            #E2E8F0;   /* 변경 없음. --bg 위 전용 */
--line-on-surface: #CBD5E1;   /* 🔴 신설(2026-08-14). --surface 위에서 --line은
                                  ΔL* 1.38로 사실상 안 보여 이 값을 대신 쓴다.
                                  직접 쓰지 말 것 — .sec-surface가 --line 자체를
                                  이 값으로 갈아 끼우므로 하위 규칙은 var(--line)만
                                  쓰면 된다 */

/* 경고 */
--warning: #DC2626;   /* 경고 전용. 폼 오류 메시지에만 */

/* 다크 잉크 — --surface-dark 위에서만, 3단계 */
--d-ink-primary:   #FFFFFF;                  /* 🔴 v3 값 rgba(255,255,255,.72)에서 재변경 */
--d-ink-secondary: rgba(255,255,255,.72);    /* 🔴 v3 값 .50에서 재변경 */
--d-ink-tertiary:  rgba(255,255,255,.50);    /* 🔴 v3 값 .28에서 재변경 */
--d-line:          rgba(255,255,255,.28);    /* 🔴 신설. 다크 배경 위 선(헤어라인) 전용.
                                                 텍스트로 쓰면 2.50:1로 AA 미달 —
                                                 선에만 쓸 것 */
```

**왜 v3와 다른가 — 접근성.** `index.html` `:root` 주석이 명시적으로 적어 뒀다:
v3 §3-1이 "정본 값 그대로 명명만 정식화"라고 적었지만 실제로는 와이어프레임의
4단계(#FFF / .72 / .50 / 선 .28)에서 #FFFFFF가 빠지고 선 알파(.28)가 잉크
3단계로 밀려 올라갔다. v3 값을 그대로 쓰면:

- tertiary(.28)로 찍히는 본문 7역할(신뢰 출처줄·플레이스홀더 라벨·푸터
  연락처·KAIST 표기·사업자값·면책문구)이 **2.50:1로 WCAG AA(4.5:1) 미달**
- 4 신뢰 섹션 H2가 #FFFFFF에서 .72로 내려가 옛 와이어프레임 C 블록 사양과 어긋남

그래서 라이브는 **이름은 v3를 따르고 값은 한 단계씩 올려 대비를 지켰다.** 이
문서(v4)는 그 라이브 값을 그대로 등재한다 — 더 이상 "정본과 구현이 다르다"는
상태가 아니다.

### 3-2. 타입 스케일 (변경 없음 — v3 그대로)

| 역할 | 모바일 | 데스크톱 | 굵기 | 행간 | 자간 | 색 |
|---|---|---|---|---|---|---|
| H1 | 36 | 56 | 700 | 1.30 | −0.035em | `--ink` |
| H2 | 28 | 40 | 700 | 1.28 | −0.030em | `--ink` |
| H3 | 22 | 28 | 600 | 1.35 | −0.025em | `--ink` |
| 카드 제목 | 18 | 20 | 600 | 1.35 | −0.02em | `--ink` |
| eyebrow | 12.5 | 12.5 | 700 | 1.5 | +0.14em | `--accent`(히어로 1회만, §3-2★ 규칙 유지) |
| 리드 | 16.5 | 18 | 400 | 1.6 | 0 | `--ink-70` |
| 본문 | 16.5 | 16.5 | 400 | 1.76 | 0 | `--ink-64` |
| 캡션 | 12.5 | 12.5 | 400 | 1.7 | 0 | `--muted` |

`index.html`의 `clamp()` 구현이 이 표와 일치함을 확인했다(`h1 { clamp(2.25rem,
4.5vw, 3.5rem) }` 등). **폰트는 Pretendard Variable 단일**, 변경 없음.

### 3-3. 간격·폭 (변경 없음 — v3 그대로, 라이브 확인)

| 항목 | 모바일 | 데스크톱 |
|---|---|---|
| 섹션 상하 패딩 | 60px | 96px |
| 컨테이너 최대 폭 | — | 1200px |
| 본문 텍스트 폭(`.body-w`) | — | 860px |
| 좁은 콘텐츠(`.narrow-c`: 결 CTA·안심문구·마감CTA) | — | 680px |

v3는 "좁은 콘텐츠"를 안심 문구·마감 CTA 2곳으로 적었으나, 라이브는 **결 CTA(7)도
같은 `.narrow-c`를 쓴다** — 신설 섹션이 옛 규칙을 그대로 물려받았다.

### 3-4. 모서리·모션 (변경 없음)

```css
--radius:     6px;
--transition: background-color .14s, border-color .14s;
```

모션은 이게 전부다. 패럴랙스·자동 캐러셀·배경 애니메이션 없음.
`prefers-reduced-motion` 대응 필수 — 라이브는 `.reveal-armed` 클래스로 스크롤
진입 페이드인을 켜고, 그 클래스가 없으면(JS 죽음) 그냥 보이는 방식으로 처리한다.

---

## 4. 컴포넌트

### C-1. 버튼 3종 (라이브 실제 카운트 — 🔴 v3 "주 CTA 3회"에서 변경)

| | 배경 | 글자 | 테두리 | 쓰는 곳(라이브) |
|---|---|---|---|---|
| **주(btn-primary)** | `--accent` | `#FFF` | 없음 | **딱 2곳** — 2 히어로, 7 결 CTA |
| **보조(btn-secondary)** | `transparent` | `--ink` | `1px --line` | 5 상품소개 탭 3개 전부, 2 히어로 "문의하기", 12 마감CTA "문의하기" |
| **텍스트(btn-text)** | 없음 | `--accent` | 없음 | 2·10·12의 "기한관리 미리보기", 9 HOW 하단 "비교해 보기" |

```css
padding: 12px 22px;
font: 600 15.5px/1;
border-radius: 6px;
```

**주 CTA는 이제 정확히 2회다**(v3의 "01 카드 CTA도 주버튼"이라던 3회 모델은
폐기). 시각사양 2 "페이지당 최대 2회" 원칙이 라이브에서 문자 그대로
지켜진다 — 5 상품소개의 탭 3개는 전부 보조 버튼이고, 9 HOW 하단과 10·12의
기한관리 트리거는 텍스트 등급이다. 이 균형을 깨지 말 것(라이브 코드주석 §7 결
CTA 참조).

### C-2. 상품소개 — 탭 3개 + 패널 3개 (🔴 전면 개편, v3의 "2층 카드" C-2를 대체)

v3의 카드 2장 그리드(6:4 비중)는 폐기됐다. 지금은 **탭 3개(사전점검·바이어
확인·기한 관리) + 패널 3개**, 아코디언도 아니고 카드 그리드도 아니다.

```
[H2] 거래를 시작하기 전에 한 번, 시작한 후에 한 번.
[리드] 지금 쓰실 수 있는 것부터 보여드립니다. 무엇을 확인해 드리는지, 하나씩 눌러보세요.

┌ 거래 시작 전 ─┐ ┌ 거래 시작 전 ─┐ ┌ 거래 시작 후 ─┐
│ 수출 사전점검 │ │ 바이어 확인   │ │ 기한 관리     │  ← role="tablist"
└──────────────┘ └──────────────┘ └──────────────┘
        ↓ 선택된 탭의 패널만 노출(hidden 속성으로 전환)
상태줄 · 요약 · (설명|훅+설명) · 불릿 · 예시화면(figure) · 실행버튼(btn-secondary)
```

- 순서는 생애주기순(거래 시작 전 · 전 · 후) 고정 — 4열 그리드 CSS가 이 순서에
  의존한다
- 기본 선택은 첫 탭. JS가 죽어도 **상품 이름 3개와 첫 상품 설명**이 그대로 보임
- **세 패널 모두 실행버튼을 가진다**(2026-08-14 tab-cta-parity-s14) — v3의 "01만
  주 CTA, 02는 보조"라는 차등은 폐기, 셋 다 `btn-secondary`로 동등
- 어느 탭에도 가격을 쓰지 않는다(v3와 동일 원칙, 계속 유효)
- 4번째 탭으로 HS코드·FTA 세율조회(구 CEPA)를 넣지 말 것 — 그것은 사전점검
  안의 세부 콘텐츠이지 독립 상품이 아니다(흐름 md §4-1)
- 동시 다중 펼침 정책은 여기 적용 안 됨(탭은 정의상 하나만 열림). QnA(C-3)는
  여전히 다중 펼침이고 그쪽은 이 변경과 무관

**레이아웃 전환(768px 미만)**: 탭 1열, 세로 스택. 카드 그리드가 아니므로 v3의
"01이 위" 규칙은 무의미해졌다(탭 순서 = 문서 순서 그대로).

### C-3. QnA 아코디언 (구성 변경, 상호작용 규칙은 v3와 동일)

3묶음 14문항 **(3+6+5)** — v3가 마지막으로 적었던 "3+5+6"(믿을 수 있나요
그룹에 "얼마인가요?" 포함)에서 재구성됐다:

- "얼마인가요?"(가격 QnA)가 3그룹에서 삭제됐다(가격 미정 정책과 연동)
- 대신 2그룹("어떻게 쓰나요")에 채널분기 문항("수출 사전점검이랑
  문의하기는 뭐가 달라요?", id `qa-14`, 2026-08-16 "셀프서브"→"수출" 정정)이
  신설돼 2그룹이 5→6문항이 됐다
- 문서 순서와 DOM id 번호가 어긋난다(`qa-14`가 2그룹 끝에, `qa-9`~`qa-13`이
  3그룹) — id는 `aria-controls` 짝만 맞으면 되므로 재번호 매기지 말 것

상호작용 규칙은 v3와 동일 — 질문=카드 제목 크기, 답=본문, 회전 셰브론(액센트
없음), 여러 개 동시 펼침 허용(하나만 열리는 방식 금지 — 비교가 안 됨), "어디에
물어볼 수 있나요" 답 안에 14 기관 안내 앵커.

### C-4. 기관 목록 (변경 없음 — v3.1 순서 그대로 라이브에 적용됨)

```
· 관세청 관세평가분류원
· KOTRA 해외무역관
· FTA활용지원센터
· 한국무역협회 Trade SOS
· 중소벤처기업부 비즈니스지원단
```

기관 유형순(공공기관→협회→정부부처) 고정, 로고 금지, 추천·평점·정렬 순위 표기
금지 — v3와 동일. 라이브는 외부 링크도 걸지 않는다(액센트색 반복 노출 방지,
"로고 금지"와 같은 이유).

### C-5. 근거 섹션 이미지 (🔴 2컷 → 1컷으로 축소)

v3는 "전체 결과지 1장 + 근거 5필드 확대 크롭 1장" 2컷을 이 섹션에 뒀지만,
라이브는 전체 결과지 컷을 **C-2(상품소개 탭1)로 옮기고**, 이 섹션(6 근거)에는
근거 5필드 확대 크롭 1컷만 남겼다. 사유: "탭1은 무엇을 받는가(대조 결과), 근거는
무엇을 근거로 비교하는가(참조 서식 인용 질문)"로 역할을 나눴다 — 두 컷을 서로
바꾸지 말 것.

- 다크 배경이 아니라 `--surface`(§2-1 배경 순서 참조) — v3는 "다크 배경 위"라고
  적었지만 근거 섹션이 신뢰(다크)에서 분리되며 배경도 함께 바뀌었다. 이미지
  테두리는 `--line`
- 목업 금지. 실물 캡처만(가상 NDA로 생성, 개인정보 0건)
- **카운터·숫자 자리는 여전히 비워둔다** — 대신 4 신뢰 섹션에 무역보험공사
  인용 문장 1개가 그 미결(구 L-1)을 대체한다(§9 참조)

### C-6. 사전 등록 폼 (🔴 목적선택 신설, 문서선택 삭제)

```
무엇을 보내실까요? (라디오, 기본값=기한관리 출시 알림 받기)
○ 기한관리 출시 알림 받기   ○ 문의하기

[이름*]  [이메일*]  [회사명(선택)]

☑ 개인정보 수집·이용 동의 *  (필수 문구 확정 — v3 시점 미결이었던 것이 해소됨)
☐ 서비스 소식 수신 동의 (선택)

[ 사전 등록하기 ]
```

- v3의 "어떤 문서를 대조하고 싶으신가요?" 체크박스 그룹(NDA/매매계약서/
  견적서·PI/기타)이 **삭제됐다** — 문의하기 유입까지 받는 폼이 되면서 문서
  종류 질문의 자리가 없어졌다
- 동의 체크박스가 1종(개인정보처리방침, 필수)에서 **2종**(개인정보 수집·이용
  동의(필수) + 서비스 소식 수신 동의(선택))으로 늘었다. 두 동의는 근거 법이
  달라 하나로 묶지 않는다
- 필수 표시는 `*`, 기본 체크 금지 — v3와 동일
- 「필수」라는 단어는 여전히 쓰지 않는다(금지어, §8)

### C-7. 경험담 (신설 — v2/v3에 없던 컴포넌트)

```
[eyebrow-quiet] 먼저, 있었던 이야기부터

┌ 처음엔 이렇게 ─┐   ┌ 그런데 실제로는 ─┐
│ "표준 양식이   │   │ 관할조항 사례     │
│  겠지…"        │   │ (본문 3~4행)      │
└───────────────┘   └──────────────────┘

이 조항, 본 적 있으신가요?

모르시는 게 당연합니다.
(본문 2~3행)

※ 예시 캡션
```

- 좌우 2단 비교 카드 + 질문 1행 + 공감 블록 + 예시 캡션의 4단 구조
- **본문은 전부 플레이스홀더**(라이브 코드주석 `TODO-REAL-CASE`) — 실제 고객
  사례가 아니다. 실제 사례로 교체 시 캡션도 함께 손볼 것(캡션만 남기고 내용만
  바꾸면 거짓 표기가 됨)
- 평가어(위험·불리·문제·잘못) 금지 — 조항의 이름과 뜻만 서술, 판정은 하지 않음
- 배경은 `--surface`(2 히어로와 4 신뢰 사이, bg→surface→dark 3단 교차를
  완성하는 자리)

### C-8. 결 CTA (신설)

```
[H2] 받으신 서류부터, 하나 확인해 보세요.
[btn-primary] 비교해 보기
[마이크로] 로그인 없이 바로
```

`.narrow-c`(680px) · 중앙 정렬 · 리드 문장 없음(바로 아래 8 안심 문구와 톤이
겹치므로). 이 섹션의 주버튼이 히어로와 함께 페이지 전체 주 CTA 2회를 채운다 —
C-1 참조.

---

## 5. 반응형 규칙 (변경 없음)

**H1·H2·H3만 스케일한다. 나머지는 전부 고정.** (표는 §3-2와 동일, 배수도 동일)

**레이아웃 전환(768px 미만, 갱신)**

| 컴포넌트 | 768px 미만 |
|---|---|
| 상품소개 탭 | 1열 세로 스택(C-2) |
| HOW 3단계 | 1열 세로 |
| 근거 이미지 | 1컷이라 스택 규칙 자체가 무의미해짐(C-5) |
| 경험담 좌우비교 | 1열 세로 스택 |
| 폼 입력 3개 | 1열 |
| 기관 목록 | 그대로(이미 세로) |

---

## 6. 🔴 색이 판정하면 안 된다 (원칙 변경 없음)

**이 제품의 핵심 원칙은 「판단하지 않는다」이다.** 대조/비교 결과에서 「다른
부분」을 표시할 때 초록·빨강을 쓰지 않는다. 밑줄·굵기·배경 톤·좌우 배치 같은
무채색 수단으로 처리한다.

| 🚫 금지 | 이유 |
|---|---|
| 초록 · 빨강 | 합격 / 불합격 신호 |
| 노랑 · 주황 | 경고 코드 |
| 보라 | AI 신호 |

`--warning`(#DC2626)은 팔레트에 있지만 **경고 전용**이고, 라이브에서는 폼 오류
메시지에만 쓴다(§3-1).

---

## 7. 🔴 준거법·관할 — 해소 경위 (v3의 §7 설계안은 채택되지 않음)

v3 §7은 "대조한 항목 / 대조하지 않은 항목"으로 화면을 구조적으로 분리하는
설계안을 냈고, §12에 "L-1 미결 — 제품 PRD 확인 필요"로 남겨 뒀다. **라이브는
다른 경로로 이 문제를 해소했다** — 제품 결과지 코드(`lib/precheck/nda/
result-view.ts` ⑤ 분리 항목)가 준거법·체결형식을 애초에 비교 상태 없이
"미포함"으로만 표시하도록 짜여 있어, v3가 우려한 "「다름」으로 찍히는" 상황
자체가 발생하지 않는다(라이브 코드주석, 6 근거 섹션 머리주석).

v3의 "대조하지 않은 항목" 별도 블록 UI는 랜딩·결과지 어디에도 구현되지 않았다
— 채택되지 않은 설계안으로 남는다. 4상태 체계 매핑 문제(구 L-1)는 이 경위로
**사실상 해소**된 것으로 보이나, 제품 PRD 쪽의 공식 확인 여부는 이 저장소
문서만으로는 알 수 없다(§12 미결 L-1 갱신 참조).

---

## 8. 하지 말 것 (v3 목록 유지 + 라이브에서 확인된 신설 항목)

| | |
|---|---|
| 🚫 | **문구 임의 수정.** 법적 검토를 거친 문장. 한 단어도 바꾸지 않는다 |
| 🚫 | 성능 수치 · 퍼센트 · 완료율 · 정확도 (숫자는 건수만) — QnA "다른 부분을 항목별로 표시해 드립니다"가 이 원칙으로 2026-08-16 수정된 실사례 |
| 🚫 | 초록 · 빨강 · 노랑 · 주황 · 보라 (§6) |
| 🚫 | 기관 로고 사용 (제휴 오인) |
| 🚫 | 기관 안내에 추천 · 평점 · 정렬 순위 |
| 🚫 | 상품소개 어느 탭에도 가격 노출 (v3 "02 카드"에서 "탭 전체"로 확장) |
| 🚫 | 「ICC」 · 「국제표준」 표기 |
| 🚫 | 목업 스크린샷 (실물만) |
| 🚫 | 「필요한」 · 「필수」 · 「정확히」 (금지어 사전) |
| 🚫 | 한 화면에 두 레퍼런스 혼용 |
| 🆕 🚫 | 「상담」— 유상 자문으로 오인된 이력. 헤더·CTA·QnA 어디에도 쓰지 않음(라이브 코드주석, 12 마감 CTA·13 폼) |
| 🆕 🚫 | 금액·오픈월 표기 (10 준비 중인 것) — `test/roadmap-no-price.test.js`가 회귀를 막음 |
| 🆕 🚫 | 「무료」(상품소개 탭2 바이어 확인) — 덤처럼 보여 가치 저평가. 「포함」 계열로 씀 |

---

## 9. 유지할 것 / 새로 확인된 것

- 본문 행간 **1.76**, 헤딩 자간(§3-2 역할별 표), **Pretendard Variable 단일**
  폰트, radius **6px**, transition **0.14s** — 전부 라이브에서 값 그대로 확인
- HOW 섹션 번호 마커 **01 / 02 / 03** — 유지
- 안심 문구 블록 — 성격은 유지, **위치는 히어로 직하가 아니라 결 CTA 뒤**(§2-2
  참조, 우선순위가 뒤집힘)
- 이미지는 여전히 **소수 정예** — 5 상품소개 3장(패널당 1장) + 6 근거 1장,
  전부 `loading="lazy"`
- 🆕 `--surface`가 v3보다 한 단계 더 진해졌다(§3-1) — "얼룩지지 않는 선에서
  최대한 진하게"가 실측으로 두 번 조정된 결과. 더 내리지도, #F1F5F9로
  되돌리지도 말 것
- 🆕 다크 배경 위 잉크 3단계는 **정본(v3) 등재값이 아니라 라이브 값을
  따른다**(§3-1) — WCAG AA를 충족하는 쪽이 우선

---

## 10. 레퍼런스 (변경 없음)

**구조·타이포·배치는 anaconda.com 홈. 색은 참고하지 않는다.**

가져오는 것: 헤딩만 스케일 · 배경 교차 + 다크 섹션 · 콘텐츠 폭 이중 구조 ·
부정형 헤드라인 · 정렬 교차. 가져오지 않는 것: 초록·보라 액센트 · Poppins+Inter
페어링.

화면 유형별: 브리핑·결정 1개형 = TOSS / 목록형 = Linear / 밀도 높은 상세 =
Stripe. **한 화면에 두 레퍼런스를 섞지 않는다.**

---

## 11. 산출물·일정

v3의 8/13~9/5 일정표는 전부 지났거나(오늘 2026-08-16 기준) 이 저장소만으로는
진행 여부를 확인할 수 없다 — 최신 일정은 이 문서가 아니라 프로젝트 관리 도구를
확인할 것(§12 미결). 기술 조건만 라이브 기준으로 갱신한다:

- Next.js + Tailwind 대신 **정적 HTML 빌드**(`scripts/build-static.js`)로
  구현돼 있다 — `site.config.json`의 `{{biz.*}}`·`{{precheck.*}}` 토큰을 빌드
  시점에 치환해 `dist/`에 쓴다. 저장소 구조상 trops_a(제품)의 TypeScript
  상수를 런타임에 참조할 수 없어, 상수 동기화는 손으로 한다(`site.config.json`
  머리주석)
- 이미지 4장(패널 3 + 근거 1), 전부 `loading="lazy"` — v3의 "이미지 0장"
  이점은 이제 없다. 대신 소수 정예(4장)로 로딩 비용을 억제하는 쪽으로
  방향이 바뀌었다

---

## 12. 미결

| # | 항목 | 상태 |
|---|---|---|
| L-1 | 「대조하지 않음」의 4상태 매핑 | §7 경위로 **사실상 해소**(다른 설계로 우회) — 제품 PRD 쪽 공식 확인 여부는 미확인 |
| L-2 | 제품 이미지 캡처 + 크롭 지점 | **해소** — 라이브에 4장 전부 반영돼 있음 |
| L-3 | 통신판매업신고번호 수령 | **미해소** — `site.config.json`에 여전히 "신청 중"(2026-08-16 확인) |
| L-4 | 개인정보처리방침 · 이용약관 본문 | 개인정보처리방침(/privacy)·환불규정(/refund) 링크는 살아있으나, **이용약관 링크가 푸터에서 빠졌다** — 페이지가 없어진 것인지 의도적 제외인지 이 저장소 문서만으로는 판단 불가(문구 확정본 v4.0 §15 미결과 동일 항목) |
| L-5 | 영문 길이 차이 반영 (`/en`) | 미확인 — 이번 갱신 범위는 국문(index.html) 한정, `/en`은 별도 확인 필요 |
| 🆕 L-6 | 배지·통계 목표치(구 "90일 20건") | 라이브 문구 어디에도 없음 — 지금도 유효한 목표인지 이 저장소 문서로는 확인 불가 |
| 🆕 L-7 | CEPA 복원 여부 결정 보류 | 별도 재논의 필요 — 2026-08-16 마무리 정리에서는 다루지 않았다. C-2·§2 표의 "CEPA는 삭제" 서술은 현재 상태 기록일 뿐 최종 결정이 아니다 |
