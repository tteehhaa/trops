# 랜딩 작업 문서 — 어느 것이 현행인지

2026-08-11 기준. **파일이 여러 버전 있습니다. 아래 「현행」만 보십시오.**

## 현행 정본 4개

| 층 | 현행 파일 | 무엇을 정하는가 |
|---|---|---|
| **문구** | [`copy/trops_랜딩_최종문구_확정본_v3.1.md`](copy/trops_랜딩_최종문구_확정본_v3.1.md) | 화면에 나가는 모든 문장. **원문이 우선** |
| **시각·구조** | [`design-master/trops_landing_design_master_v3.md`](design-master/trops_landing_design_master_v3.md) | 토큰 · 컴포넌트 · 12섹션 구조 · 정렬·배경 |
| **시각 실측 근거** | [`design-master/trops_landing_visual_spec_v1.md`](design-master/trops_landing_visual_spec_v1.md) | 위 정본 값이 **왜 그 값인지** — Anaconda 대비 실측과 V1~V7 적용 순서 |
| **레이아웃 확인** | [`wireframe/TROPS_랜딩_와이어프레임_v3.html`](wireframe/TROPS_랜딩_와이어프레임_v3.html) | 6섹션 상세 + 12섹션 정렬 리듬 맵 |

브라우저로 와이어프레임을 열면 섹션별 토큰 사용 내역과 남은 [문의] 항목이 함께 보입니다.

시각사양 v1 과 디자인 정본 v3 은 토큰·패딩 값이 서로 같습니다. 어긋나 보이면 정본 v3 이
우선이고, 시각사양은 근거를 확인하는 용도로 보십시오. 다만 시각사양 4-1 배치도는 옛 9섹션
구조 기준이라 **지금 12섹션과 다릅니다** — 의도(`--surface` 하향 · 다크 섹션 페이지당 1회)만
읽고 섹션 목록은 정본 v3 §2 를 보십시오.

## 우선순위

문구와 디자인 정본이 어긋나면 **문구 확정본이 이깁니다** (정본 §0).
정본 §C-2 의 H2 예시가 확정본 §4 와 다른 것이 실제 사례입니다 — 확정본을 썼습니다.

**문구는 한 글자도 수정하지 않습니다** (정본 §0 · §8). 바꿔야 할 이유가 생기면
바꾸지 말고 마케팅팀에 문의하십시오. 줄바꿈(`<br>`)·링크(`<a>`) 추가는 문구 변경이
아니라 마크업이라 정본 §2-3 이 명시적으로 허용합니다.

## 직전 버전 (참고용 · 따르지 마십시오)

| 파일 | 현행과의 차이 |
|---|---|
| `design-master/trops_landing_design_master_v2.md` | v3 에서 §C-3(13→14문항) · §C-4(기관 순서) · §3-1(`--d-ink-*` 등재)이 바뀜 |
| `copy/trops_랜딩_최종문구_확정본_v3.md` | v3.1 에서 §8·§11 기관 나열 순서만 정정(문구 자체는 동일) |
| `wireframe/TROPS_랜딩_와이어프레임_dc_v2.html` | v3 이전 판 |
| `wireframe/design_archive/` · `design_handoff_*/` | 초기 핸드오프. 현재 구현과 어긋납니다 |

## 🔴 정본 §3-1 `--d-ink-*` 값이 구현과 다릅니다 — 정본 쪽 수정 대기

v3 에서 등재한 세 값을 그대로 쓰면 **본문 7역할이 WCAG AA 미달**이 됩니다.
코드는 이름만 정본을 따르고 값은 대비를 지키는 쪽으로 뒀습니다
(`index.html` `:root` 주석에 같은 내용이 있습니다).

| 토큰 | 정본 §3-1 등재값 | 구현값 | 정본값의 `#0F172A` 위 대비 |
|---|---|---|---|
| `--d-ink-primary` | `.72` | `#FFFFFF` | 9.60:1 ✓ (다만 §6 H2 가 흰색에서 내려감) |
| `--d-ink-secondary` | `.50` | `.72` | 5.23:1 ✓ |
| `--d-ink-tertiary` | `.28` | `.50` | **2.50:1 ✗ AA 미달** |

정본은 "값 자체는 변경 없음, 명명만 정식화"로 적었지만 실제로는 한 단계씩 밀렸습니다 —
와이어프레임이 쓰던 4단계(`#FFF` / `.72` / `.50` / 선 `.28`)에서 `#FFFFFF` 가 빠지고
선 알파(`.28`)가 잉크로 올라왔습니다.

**제안**: §3-1 을 잉크 4단계로 늘리고(`#FFFFFF` 복원) `.28` 은 선 전용
(`--d-line`)으로 되돌리십시오. 와이어프레임 C 블록이 §6 H2 를 `#FFFFFF` 로
명시하고 있어 정본 내부끼리도 어긋난 상태입니다.

## 구현 상태 (2026-08-11)

12섹션 전부 구현됐습니다. 남은 것은 셋입니다.

| 미결 | 내용 | 대기 |
|---|---|---|
| **이미지** | 06 신뢰 섹션 이미지 2컷 | 실물 캡처 (정본 미결 L-2) — 아래 방향 변경 참고 |
| **선행 결정** | 준거법·관할의 4상태 매핑 | 정본 §7 · 미결 L-1 |

### 「대조」 vs 「비교」 — 확정됐습니다 (2026-08-11 · 확정본 v3.2 · Option A)

트롭스_a 확인 완료. **전체 치환하지 않습니다.** 층마다 쓰는 단어가 다릅니다.

| 층 | 쓰는 말 | 왜 |
|---|---|---|
| 랜딩 (`index.html`) | **비교** | 마케팅 언어 |
| 제품 앞단·본체 (`precheck`·`nda`·`uae`·`api/`) | **대조** | 법적 경계 언어 |

접점은 §8 「무엇을 기준으로 비교하나요」 답변 마지막 줄이 설명합니다 —
`(실제 서비스 화면에서는 「대조」라는 표현을 씁니다.)`

**임의로 통일하지 마십시오.** 어느 쪽으로든 맞추면 위 경계가 무너지고 그 한 줄이
근거를 잃습니다. 랜딩 안에 아직 「대조」가 남은 자리(HOW 섹션 · nav 「대조 방법」)는
확정본 §5 로 교체되지 않은 구절이라 남은 것이고, 별도 배치 몫입니다.

### 06 신뢰 섹션 — 캡처 방향이 바뀌었습니다 (2026-08-11)

트롭스_a 가 정본 §7 준거법 문구를 이번 캡처 라운드에서 **완전히 빼고** 결과지
6개 화면(C-03)을 새로 만드는 쪽으로 방향을 틀었습니다. 캡처 시점이 늦어질 수
있으므로 플레이스홀더를 계속 유지합니다.

플레이스홀더 문구는 **「실제 서비스 화면 · 준비 중」 한 줄뿐입니다**(/en 은
`Actual service screen · coming soon`). 2026-08-11 에 정리했습니다.

⚠️ 여기에 날짜·담당 부서·크롭 지점·화면비를 **다시 넣지 마십시오.** 이 자리는
주석이 아니라 화면에 보이는 본문이라 빌드의 주석 제거가 걷어내지 못합니다.
실제로 「8/20 수령」·「크롭 지점 마케팅팀 표시」가 trops.kr 에서 그대로 읽히던
상태였습니다. **캡처 일정은 이 문서에만 적습니다** — 이 폴더는 `.vercelignore`
라 배포되지 않습니다.

### 히어로 eyebrow — 확정본 §2 와 일부러 다릅니다 (2026-08-11)

| | 확정본 §2 | 구현 |
|---|---|---|
| 국문 `index.html` | 수출을 앞두고 / BEFORE YOU EXPORT | **수출을 앞두고** |
| 영문 `/en` | — | BEFORE YOU EXPORT (단독) |

영문 태그는 `/en` 히어로가 단독으로 쓰고, 국문판은 국문만 씁니다. 국문 히어로
eyebrow 와 푸터 §12 태그가 이제 글자까지 같아져 페이지 처음과 끝이 같은 말로
묶입니다. **영문을 다시 붙이지 마십시오.**

## ⚠️ 배포에서 제외됩니다

이 폴더는 `.vercelignore` 에 있습니다. 가격 · 미결 법무 항목 · 내부 일정 ·
금지어 목록이 그대로 들어 있어 공개되면 안 됩니다. 그 줄을 지우지 마십시오.
